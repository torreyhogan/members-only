<% provide(:title, 'Sumbit Post') %>
<h1>New Post</h1>

<div class="row">
	<%= form_for(@post) do |f| %>
		<%= f.label :content %>
		<%= f.text_field :content %>

		<%= f.submit "Submit Post" %>
	<% end %>
</div>